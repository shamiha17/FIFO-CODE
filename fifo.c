#include <stdio.h>int main() {    int pages, frame, flag, hit = 0, miss = 0, pos = 0;    printf("Enter number of pages: ");    scanf("%d", &pages);    int page[pages];    printf("Enter page sequence: ");    for(int i = 0; i < pages; i++)        scanf("%d", &page[i]);    printf("Enter number of frames: ");    scanf("%d", &frame);    int frames[frame];    for(int i = 0; i < frame; i++)        frames[i] = -1; // initialize empty frames    printf("\nPage Replacement Process:\n");    for(int i = 0; i < pages; i++) {        int current_page = page[i];        flag = 0;        // Check if page already in frame        for(int j = 0; j < frame; j++) {            if(frames[j] == current_page) {                hit++;                flag = 1; // page hit                break;            }        }        // Page fault: replace using FIFO        if(flag == 0) {            frames[pos] = current_page;            pos = (pos + 1) % frame; // circular increment            miss++;        }        // Print frame status        printf("After processing page %d: ", current_page);        for(int j = 0; j < frame; j++) {            if(frames[j] != -1)                printf("%d ", frames[j]);        }        printf("\n");    }    printf("\nTotal Page Hits: %d\n", hit);    printf("Total Page Misses: %d\n", miss);    return 0;}